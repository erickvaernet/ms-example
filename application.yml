global-mesagge: its works

spring:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: '*'
  info:
    env:
      enabled: true
